# wiki-ingest.env.example — DeepSeek 摄取 key 本地模板（feat-wiki-auto-ingest-b2 · U2）
#
# 用途：wiki 摄取（wiki-ingest-cheap / wiki-auto-ingest）需 DEEPSEEK_API_KEY。
#       env 已注入时优先用 env；env 缺失时脚本从同目录的 wiki-ingest.env 加载 key
#       （一次写入、长期复用，免每次会话手动注入）。
#
# 用法：
#   1. 复制本模板为同目录下的 wiki-ingest.env：
#        cp wiki-ingest.env.example wiki-ingest.env
#   2. 编辑 wiki-ingest.env，填入 DEEPSEEK_API_KEY 的真实值。
#
# 安全（GEN-3 / R-005 · 密钥绝不入库）：
#   - wiki-ingest.env 已被 .gitignore 的 *.env 规则忽略、绝不入库；真实 key 只留本地。
#   - 本 .example 模板不含真实 key，可安全提交入库。
#   - sync_public_marketplace.sh 组装公有仓副本时剔除所有 *.env 实 key 文件（保留 *.env.example）。

DEEPSEEK_API_KEY=

# 可选：覆盖摄取模型档（默认 deepseek-v4-flash；无人值守要零返工可设 deepseek-v4-pro）。
# WIKI_ENGINE_INGEST_MODEL=deepseek-v4-flash

# 可选：单次自动摄取批上限（默认 3 · 温和默认防会话结束后自动摄取过激）；delta 超上限时余量留下次触发续摄。
# 积压多、想加速清债可上调（如 =20）。
# WIKI_ENGINE_AUTO_INGEST_MAX_BATCHES=3
