# sync-manifest — 镜像分层刷新分类单源（fix-mirror-upgrade-propagation-20260714 · T1 · FR-2）
#
# 消费方：session_start_mirror_sync.sh（经 scripts/lib/mirror_sync_lib.sh 解析）+ harness_bootstrap.sh（组件 opt-out 探测）。
# 权威源 = plugins/harness-core/（= 消费方 $CLAUDE_PLUGIN_ROOT）；镜像 = 消费方 .harness/。
#
# 行格式（扁平 · bash/grep/sed 可解析 · 零外部解析器 · bash 3.2 兼容）：
#     <class>  <glob>  [opt-out]
#   - <class> ∈ machine | semi | custom
#   - <glob>  = 权威源根相对路径 glob（在 shell `case` 语义下匹配，`*` 递归匹配含 `/`）
#   - opt-out（可选第三字段）= 该 glob 默认不刷新（保留登记、改一行即重新纳入）
#   - `#` 起始行 = 注释；空行忽略
#
# 三分类策略（spec §2）：
#   machine（覆盖刷新）：权威源较新内容覆盖镜像（受哈希台账本地改动保护）。
#   semi（半定制件）：覆盖后对含 HARNESS:IDENTITY 标记块的文件重跑 init_identity 回填身份段（临时文件+原子替换）。
#   custom（定制件）：永不自动覆盖；检测上游模板变化→产出「待处理级联评估」提示留人裁决。
#
# 匹配优先级不变量（FR-2 · SH-3 · 消费端 mirror_sync_classify 实现）：
#   ① custom 类恒优先于任何 machine/semi glob（custom 恒胜）；
#   ② 其余重叠取最长（最具体）path glob 匹配胜出。
#
# 镜像落点映射（消费端 mirror_sync_dest_rel）：默认 `X` → `.harness/X`；
#   例外 `_template/_TEMPLATE/X` → `.harness/changes/_TEMPLATE/X`。

# ── 机器件（machine · 覆盖刷新）──
machine  components/*
machine  scripts/*
machine  skills/*
machine  workflows/*
machine  _template/_TEMPLATE/*

# rules/ 分类口径（spec §2.1 · SH-5）：除 4 定制件外全部机器件（现 12 文件）+ 缺省规则（未来新增 rules 默认落 machine）。
# 现 12 机器件 rules（经 rules/* 缺省 machine 兜住 · 逐一登记备审）：
#   开发流程规范.md 文档wiki查询与摄取规范.md codegraph查询路由与精度规范.md 验收回填与证据捕获规范.md
#   体系设计原则.md 测试守护规范.md 验收执行规范.md CLI调用规范.md CLI调用规范-codegraph.md
#   wiki产物数据规范.md 开发流程操作教训.md README.md
machine  rules/*

# ── config（machine 类 · 覆盖刷新 · fix-sync-manifest-config-gap-20260716）──
# config/ = 机器可读权威表目录（当前 model_tiers.tsv：阶段→默认模型档单源），供宿主侧委派守护
#   hook pretool_agent_delegation_guard.sh 读 model_tiers.tsv 做「按阶段默认档降档校验」（deny 降档漂移）。
#   guard 候选② `$root_dir/.harness/config/model_tiers.tsv` 需由 mirror 铺到消费方，故此表须 machine 覆盖
#   语义（权威单源、消费方不定制），每会话以权威源覆盖镜像（受哈希台账本地改动保护）。
# 同族第二次复发留痕：parent card fix-consumer-harness-config-gap-20260714（HARNESS_CONFIG 模板，见文末
#   opt-out 区）为第一次；本卡 fix-sync-manifest-config-gap-20260716 为第二次——均为「新增 machine 可消费
#   产物时忘了扩 sync-manifest → 消费方永不拿到 → 宿主侧功能 fail-open 空转」。教训：**新增任何 machine
#   可消费产物（宿主 hook/脚本会读的权威表/组件）必须同步在本 manifest 扩一条对应 glob**，否则镜像不铺、
#   消费方缺文件、依赖它的守护静默空转。config/* 子树整体纳入（非精确单文件），防未来新增 config 表第三次复发。
machine  config/*

# ── wiki-background/harness-lessons（machine 类 · 覆盖刷新 · feat-memory-distillation-to-plugin-20260716）──
# harness-lessons/ = 背景库 corpus（从 MEMORY 蒸馏的跨会话工程教训 8 篇 md + index.md），供设计/评审阶段
#   子 Agent 经 wiki-query --wiki .harness/wiki-background/harness-lessons/ 旁路检索。权威源单源、消费方不
#   定制，故 machine 覆盖语义，每会话以权威源覆盖镜像（受哈希台账本地改动保护）。
# 镜像语义：plugin 根相对 wiki-background/harness-lessons/X → 消费方 .harness/wiki-background/harness-lessons/X
#   （dest_rel 默认映射）。case 语义下末尾 `*` 匹配含 `/`，命中该目录下全部文件；scan_roots 取首 `*` 前段
#   = wiki-background/harness-lessons 作遍历根，find -type f 递归枚举全部 corpus 文件。
# ⚠ glob 精确到 harness-lessons/ 子目录（非泛 `wiki-background/*`）= defense-in-depth（非机制必需 · 已根治）：
#   历史缺陷（v0.9.0 前 · failure-record-005）：mirror_sync_classify/scan_roots 曾用 `set -- $line`（无引号）
#   解析 manifest 行 → glob 先经**宿主 cwd 路径展开（pathname expansion）**。宿主 hook
#   （session_start_mirror_sync.sh `cd $TOP` 仓库根）运行、而仓库根**存在** wiki-background/ 目录（C 源背景库
#   约定：research-<topic> 语料落仓库根 wiki-background/）→ 泛 `wiki-background/*` 被展开成实文件、glob 退化为
#   具体文件名 → 嵌套 corpus 路径 wiki-background/harness-lessons/*.md 分类 miss → corpus 永不铺到消费方。
#   ✅ 已在解析层根治（v0.9.0/v0.9.1 · failure-record-005）：两处解析改 `read -r <<< "$line"`（quoted
#   here-string）→ 分词天然不做 pathname expansion，泛 glob 现已可安全使用、不再依赖「glob 前缀不与宿主 cwd
#   目录撞名」这一隐含前提。本条 harness-lessons/ 精确前缀因此**降级为 defense-in-depth**（缩小 scan_roots
#   遍历面 + 避免误纳邻近 corpus），而非规避 cwd 撞名的机制必需。解析层非展开的回归护栏见
#   tests/test_sync_manifest_glob_parse_guard.py（解析层若回退 `set -- $line` 即转红）。
# 同族第三次复发防线（承 config/* §）：未来新增背景库 corpus 目录须同步在本 manifest 扩一条对应 glob
#   （同理精确到该 corpus 子目录避 cwd 撞名），否则镜像不铺、消费方缺 corpus、依赖它的旁路检索静默空转。
machine  wiki-background/harness-lessons/*

# ── transcript-archive 产物（machine 类 · feat-transcript-archive-20260717 · T6/AC-6 逐一枚举防泛称漏项）──
# 本卡 T1-T5 新增 5 个具名 machine 双副本产物（各含 plugins/harness-core/ 权威源 + .harness/ 镜像）。
# AC-6 三处同一具名清单（sync-manifest / MIRROR_FILES / AC-6 断言）逐一枚举，消泛称漏项（v0.8.11 族教训）：
#   1. scripts/transcript_archive.py          （CLI 主程序 · T3）
#   2. scripts/lib/transcript_lean.py         （lean 提取+脱敏库 · T1）
#   3. scripts/lib/transcript_index.py        （by-instance 索引库 · T2）
#   4. scripts/transcript_archive_cron.sample （cron 样例 · T5）
#   5. hooks/session_end_transcript.sh        （SessionEnd hook · T5）
# 前 4 项落 scripts/ 子树、**已由上方 `machine scripts/*` glob 覆盖分类并铺送**（同 rules/ 现 12 机器件
#   逐一登记备审惯例：glob 已铺、此处显式枚举精确条目备审 + 供 AC-6 grep 断言命中；不重复落功能行——
#   若为 scripts/lib/*.py 落精确功能条目会令 scan_roots dirname 派生出重叠 `scripts/lib` 扫描根、与既有
#   `scripts` 根令同文件被 find 双遍历重复处理，故此处仅注释枚举、复用 `scripts/*` 单根）。
# 第 5 项落 hooks/ 子树——**上方无任何 hooks glob**（plugin hooks/ 下 ~20 个 hook 经 plugin hooks.json
#   原生分发、非 .harness 镜像管理，现 .harness/hooks/ 仅此 1 个镜像文件），故此为真缺口：须显式登记方能
#   把该单 hook 铺到消费方 .harness/hooks/。
# ⚠ 禁 `hooks/*` 泛 glob（两重原因）：① 泛 glob 会把 plugin hooks/ 下全部 ~20 个 hook 误分类 machine、
#   全量扫进消费方 .harness/hooks/（越界污染——那些 hook 无 .harness 镜像、也不该有 · 此为**当前主因**）；
#   ② 历史上泛 glob 曾有宿主 cwd pathname-expansion 撞名退化面（wiki-background/* 教训 · 解析层 v0.9.0/v0.9.1
#   `read -r` 已根治，见上方 wiki-background 段；现仅作 defense-in-depth 考量、非活跃风险）。精确单文件路径
#   （无 `*` 元字符）双免：read -r 分词天然不展开 → 不被 cwd 撞名；scan_roots 取 dirname=hooks 遍历、classify 仅该文件精确命中、
#   同目录其余 hook 落 unclassified 不铺。既有 `HARNESS_CONFIG.yaml.template`（下方 opt-out 区）即精确单文件登记先例。
# skill/命令类新增（skills/transcript-archive/** · commands/transcript-archive-enable.md）按 manifest 既有
#   分类惯例**已被现况 glob 覆盖、无须新增条目**：skills/transcript-archive/** 由上方 `machine skills/*` 覆盖
#   （machine · 刷新铺送）；commands/transcript-archive-enable.md 由上方 `machine commands/* opt-out` 覆盖
#   （machine · opt-out 保留登记不刷镜像 · 与既有 commands 分类惯例一致）。
machine  hooks/session_end_transcript.sh

# ── 半定制件（semi · 覆盖 + 身份回填）──
# agents/* 全归 semi：含 HARNESS:IDENTITY 标记块的（application-owner.md）覆盖后重跑 init_identity 回填身份段；
# 无标记块的（reviewer/generator/strategist/entropy/README）semi 处理器等价纯覆盖（无身份段可回填）。
semi  agents/*

# ── 定制件（custom · 永不覆盖 · custom 恒胜任何上方 machine/semi glob）──
# 注：API设计规范.md / 架构规范.md 虽存于 rules/，其内容为项目派生模板、消费方按项目定制，故归 custom（安全侧 · SH-5）。
custom  rules/工程结构.md
custom  rules/项目编码规范.md
custom  rules/API设计规范.md
custom  rules/架构规范.md

# ── commands（machine 类 · 默认 opt-out · UQ-3 v2 限定 · SH-6 / HITL-2 C-2 认可）──
# 消费落点核实（T1 子项 · 2026-07-14）：镜像目标 .harness/commands/ 现无运行时消费方
#   （Claude Code 读 .claude/commands/ 与 plugin manifest，不读 .harness/commands/；plugin 原生 commands
#    分发已直达消费方，无 E-1 类缺口）→ 直刷镜像=死刷新 → 保持 opt-out。
# 未来 .harness/commands/ 出现真消费点时，删除下行 `opt-out` 一字即重新纳入刷新。
machine  commands/*  opt-out

# ── HARNESS_CONFIG 模板（machine 类 · 默认 opt-out · fix-consumer-harness-config-gap-20260714 · HITL 决议）──
# 登记备审但不进 mirror 覆盖刷新：消费方项目身份 HARNESS_CONFIG.yaml 的落盘由 session-start
#   `_ensure_harness_config`（create-if-missing 从本模板复制）专管，非镜像刷新职责。此处 opt-out
#   仅使模板纳入 sync-manifest 分类台账备审，避免覆盖消费方已填实值。
machine  HARNESS_CONFIG.yaml.template  opt-out
